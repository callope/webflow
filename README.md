# WebFlow

A minimalist package to help you build powerful asynchronous
web-hooks.

## Installation

This package is available on PyPi, so you can install it with
`pip install webflow`.

## Usage

A simple example from a project of mine:

```
$ tree .
```
